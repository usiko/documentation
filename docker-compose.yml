services:
  documentation:
    image: ghcr.io/usiko/documentation:latest
    container_name: documentation
    restart: unless-stopped
    ports:
      - "9000:80"
    env_file:
      - path: .env
        required: false
